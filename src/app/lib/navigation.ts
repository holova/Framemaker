export const HOME = '/';
export const DASHBOARD = '/dashboard';

export const FRAMES = `${DASHBOARD}/frames`;
export const CREATE_FRAME = `${DASHBOARD}/frames/create`;
export const EDIT_FRAME = `${DASHBOARD}/frames/:frame`;

export const TEMPLATES = `${DASHBOARD}/templates`;

export const AUTH = '/auth';
export const LOGIN = `${AUTH}/login`;

export default {
  HOME,
  DASHBOARD,
  FRAMES,
  CREATE_FRAME,
  EDIT_FRAME,
  TEMPLATES,
  AUTH,
  LOGIN,
};
